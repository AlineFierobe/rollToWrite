import characters from "./characters/characters.store";
import constraints from "./constraints/constraints.store";
import locations from "./locations/locations.store";
import situations from "./situations/situations.store";
import words from "./words/words.store";

const listings = {
  characters,
  constraints,
  locations,
  situations,
  words,
};

export default listings;
